# UI transformation — 5 September 2026

Scope: existing FastAPI/Jinja templates and ES modules. Preserve API payloads,
session behavior, real account data, and the immersive Play/Your Emora routes.
Existing uncommitted work is the baseline.

1. Inspect authentication, shared layout, styles, runtime, and frontend contracts.
2. Recompose the shared authentication shell: editorial forest scene, readable
   form surface, compact mobile brand header, deliberate fields and actions.
3. Add shared accessible field validation and password controls; stabilize
   provider slots, loading, callback errors, and feedback.
4. Inspect major routes in the running application and fix observed shared
   overflow, dialog, navigation, and rendering issues.
5. Exercise registration/login/logout against local MongoDB with a disposable QA
   account; use browser interception only for explicitly simulated failures.
6. Check mobile widths 320–430, tablet and desktop, both themes, keyboard,
   reduced motion, accessibility, and existing Python regression/UI budgets.
7. Record evidence and remaining limitations in UI_TRANSFORMATION_REPORT.md.
