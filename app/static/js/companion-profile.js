import {apiRequest,getStoredUser,setStoredUser,showStatus,initChrome} from './common.js';
const form=document.getElementById('companion-name-form'),input=document.getElementById('companion-name-input');
let version=0;
async function load(){const response=await apiRequest('/api/product/meeting',{auth:true});version=response.version;input.value=response.profile.preferredName||'';document.getElementById('profile-account-name').textContent=getStoredUser()?.name||'your account name';}
if(form){void load().catch(error=>showStatus(document.getElementById('avatar-status'),error.message));form.addEventListener('submit',async event=>{event.preventDefault();const name=input.value.trim();if(!name){input.setCustomValidity('Enter the name you would like Emora to use.');input.reportValidity();return;}const button=form.querySelector('button');button.disabled=true;try{const response=await apiRequest('/api/product/meeting',{method:'PATCH',auth:true,body:{preferredName:name,expectedVersion:version}});version=response.version;setStoredUser(response.user);initChrome();showStatus(document.getElementById('avatar-status'),`Emora will call you ${name}. Your account name is unchanged.`,'success');}catch(error){showStatus(document.getElementById('avatar-status'),error.message);}finally{button.disabled=false;}});input.addEventListener('input',()=>input.setCustomValidity(''));}
